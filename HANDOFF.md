# Quran Watch 8 - Session Handoff 🚀

**تاريخ التحديث:** أواخر أغسطس 2026
**المشروع:** تطبيق Quran Watch 8 (للساعات الذكية Wear OS) مع لوحة تحكم PWA للمزامنة (Quran Watch Hub).
**الهدف:** أن يستمر أي مساعد ذكاء اصطناعي (AI) في تطوير التطبيق انطلاقاً من هذه النقطة دون فقدان السياق.

---

## 🎯 الإنجازات والميزات المكتملة

1. **تحويل التطبيق إلى شاشة رئيسية (Home Launcher):**
   - تم التخلي عن محاولة إنشاء "وجه ساعة" (Watch Face) بسبب قيود نظام `Wear OS 5` الصارمة ضد الوجوه المبرمجة بالـ Kotlin وفرضهم لصيغة (WFF).
   - بدلاً من ذلك، تمت إضافة خصائص `CATEGORY_HOME` و `CATEGORY_DEFAULT` إلى `MainActivity` في `AndroidManifest.xml`.
   - **النتيجة:** التطبيق الآن يعمل كتطبيق عادي **وأيضاً كواجهة نظام (لانشر)** تفتح فور الضغط على الزر الرئيسي للساعة (Home Button). 

2. **نظام البلاطات الجديد (3 صفوف مخصصة):**
   - تم تصميم شاشة رئيسية في الساعة (`HomeScreen.kt`) تعتمد على 3 صفوف: (علوي، أوسط، سفلي).
   - تم إنشاء كلاس `TileConfig.kt` لإدارة البلاطات بأوزان رياضية محددة (مثلاً 50% لكل بلاطة، أو 33% لثلاث بلاطات).
   - تم دعم البلاطات "الحية" التي تتبدل محتوياتها تلقائياً.

3. **لوحة التحكم السحابية (PWA - Web):**
   - تم بناء قسم مخصص في الموقع `pwa-web/index.html` و `app.js` للتحكم بتوزيع البلاطات (تخصيص الصفوف الثلاثة) وتغيير ألوانها.
   - تم إصلاح `Crash` كان يتسبب في إخفاء قسم البلاطات بسبب خطأ نحوي (Syntax Error) في `app.js`.

---

## 🚧 حالة العمل الحالية (أين توقفنا؟)

1. **الساعة:** 
   - تم تثبيت التطبيق بنجاح عبر `ADB`.
   - المستخدم يستطيع اختيار `Quran Watch` كشاشة رئيسية بدلاً من `One UI Watch Home`.
2. **موقع الويب (Vercel):**
   - الكود المحلي (Local) تم إصلاحه ويعمل بشكل ممتاز على `http://localhost:8080`.
   - **لم يتم رفع التحديث إلى Vercel** (`quran-watch8-hub.vercel.app`) بسبب حاجة الرفع إلى تصريح دخول (Not authorized) من جهاز المستخدم.

---

## 📋 الخطوات القادمة المطلوبة (للجلسة القادمة)

1. **التأكد من تشغيل التحديثات على الموقع السحابي:** مطالبة المستخدم بتشغيل أمر الرفع (`npx vercel --prod`) في طرفيته لكي يصل الإصلاح للساعة.
2. **اختبار المزامنة:** التحقق من قدرة الساعة على قراءة إعدادات الـ 3 صفوف بشكل صحيح من الويب وتحديث واجهتها بدون أخطاء.
3. **تطوير ميزات البلاطات:** العمل على إضافة ميزات تفاعلية لكل بلاطة (السبحة، وقت الصلاة، موضع القراءة) بحيث تفتح الأقسام المخصصة عند الضغط عليها.
4. **التنبيهات المسبقة:** التأكد من تفعيل المنبهات قبل الصلاة وفقاً للإعدادات الجديدة المرسلة من الموقع.

---
*ملاحظة للـ AI: ابدأ عملك دائماً بقراءة هذا الملف لتعرف السياق والمرحلة الدقيقة التي يمر بها المشروع.*

---

## 🎨 تنفيذ هوية Finder لصفحة الويب — سجل حي (31 أغسطس 2026)

### طلب المستخدم الحالي

- تحويل الهوية البصرية للوحة `pwa-web` إلى أسلوب مستوحى من Finder في macOS: واجهة فاتحة هادئة، شريط جانبي زجاجي، أزرق macOS كلون رئيسي، بطاقات بيضاء، وحدود وظلال خفيفة.
- بدء التنفيذ الفعلي مع الحفاظ على جميع الميزات الحالية.
- تحديث هذا الملف باستمرار حتى يستطيع وكيل آخر متابعة العمل دون إعادة الاستكشاف.
- جمع ملاحظات الكود والتصميم الخاصة بالموقع والساعة لمراجعتها مع المستخدم في نهاية العمل.

### حالة التنفيذ الآن

**المرحلة:** اكتمل التنفيذ المحلي والاختبار البصري والوظيفي (TDD / GREEN).

**الملفات المخطط تعديلها:**

- `pwa-web/index.html` — إعادة تنظيم الأقسام والتنقل.
- `pwa-web/style.css` — نظام تصميم Finder والاستجابة للشاشات.
- `pwa-web/app.js` — ربط البنية الجديدة وإصلاح الأزرار والمعاينة والمزامنة.
- `pwa-web/sw.js` — إصلاح تشغيل PWA دون اتصال.
- `pwa-web/tests/ui-contract.test.mjs` — اختبارات عقد الواجهة ومنع فقدان الميزات.

### قواعد التنفيذ المتفق عليها

1. لا تُحذف أي ميزة قائمة؛ تُنقل إلى قسم أو لوحة أو درج أوضح.
2. تبقى معرفات عناصر التحكم الأساسية ثابتة حتى لا تنكسر وظائف JavaScript.
3. تُفصل حالة المسودة عن نتيجة المزامنة بصرياً، ولا تُعرض رسالة نجاح غير مؤكدة.
4. يجب أن تعمل الواجهة على سطح المكتب والهاتف من دون تمرير أفقي.
5. تُحافظ معاينة الساعة على الدائرة وقصّ البلاطات، مع تقريب المحتوى الحي من الساعة الحقيقية.

### ملاحظات متراكمة للمراجعة النهائية

#### الموقع

- يوجد 44 اسم class مستخدماً في HTML/JavaScript بلا تعريف مقابل في CSS؛ هذا سبب رئيسي لعدم اتساق الشكل.
- محاكي الساعة ثابت بعرض `480px` ويتجاوز شاشة الهاتف: عرض المستند المقاس كان `495px` داخل viewport عرضه `390px`.
- قسم إدارة القوالب متداخل داخل لوحة الألوان بسبب إغلاق ناقص لعنصر `div` في HTML.
- يوجد نظامان متوازيان للقوالب (`builtInPresets` و`WEB_PRESETS`) ويجب توحيد سطح العرض مع الحفاظ على البيانات.
- زر إضافة البلاطة يجمع بين `onclick` و`addEventListener`، ما قد ينفذ الإضافة مرتين.
- زر تمديد البلاطة ظاهر لكنه غير مربوط بأي معالج.
- استجابة السحب السحابي مغلفة داخل `data` بينما كود الويب يبحث عن `tilesConfig` في الجذر.
- ملف `sw.js` يحتوي أسماء دوال وملفاً مشوهاً، ولذلك تثبيت/تشغيل PWA دون اتصال غير موثوق.

#### الساعة

- واجهة الساعة الفعلية تستخدم شبكة مستطيلة حادة الحواف مقصوصة داخل دائرة، وهي أساس يجب الحفاظ عليه في المحاكي.
- المحاكي يعرض أسماء الميزات الثابتة، بينما الساعة تعرض الوقت، العد التنازلي، موضع القراءة والبيانات الحية؛ يلزم تحسين التطابق.
- بعض البلاطات مثل التاريخ والطقس والبطارية تستخدم قيماً ثابتة/افتراضية في كود الساعة، وتحتاج لاحقاً إلى مصادر نظام فعلية.
- واجهة الساعة تحسب الخط نسبة إلى عرض الشاشة، بينما الويب يستخدم px مباشرة؛ يلزم توضيح أن المعاينة تقريبية أو توحيد معادلة القياس لاحقاً.

### ما تبقى في هذه المهمة

1. ~~إضافة اختبارات عقد الواجهة وتشغيلها في حالة RED.~~ تم.
2. ~~إعادة بناء HTML بالهيكل الجديد.~~ تم.
3. ~~تطبيق نظام ألوان وتخطيط Finder في CSS.~~ تم.
4. ~~ربط JavaScript وإصلاح الأعطال الوظيفية المكتشفة.~~ تم.
5. ~~اختبار سطح المكتب والهاتف بصرياً ووظيفياً.~~ تم.
6. ~~تحديث هذا السجل بالنتائج والمنجز والمتبقي.~~ تم.

### سجل التغييرات المنفذة

- أُعيد بناء `pwa-web/index.html` كنافذة Finder: شريط جانبي، شريط أدوات علوي، ومساحة محتوى مستقلة.
- نُقلت جميع الوظائف إلى سبعة أقسام واضحة من دون حذف معرفات التحكم القديمة.
- أُزيلت معالجات النقر المكتوبة داخل HTML لمنع تنفيذ الأمر مرتين وتحسين الصيانة.
- أُعيد بناء `pwa-web/style.css` بهوية Finder فاتحة: أزرق macOS، أسطح رمادية شفافة، بطاقات بيضاء وحدود وظلال خفيفة.
- أصبحت معاينة الساعة مرنة بدل العرض الثابت، وأضيف تخطيط هاتف بشريط تنقل سفلي يمنع التمرير الأفقي.
- أضيفت حالات تركيز واضحة، واحترام إعداد تقليل الحركة، وخيار إظهار المنطقة الآمنة في المحاكي.
- رُبط التنقل الجديد بعناوين وسياق مختلف لكل قسم، وربطت الاختصارات السريعة بالصفحات الصحيحة.
- أُضيفت قائمة طبقات حية للمصمم، وقيم معاينة حقيقية/تقريبية للوقت والتاريخ والصلاة والبطارية والطقس والقراءة.
- رُبطت أزرار الإضافة يمينًا/يسارًا/في صف جديد، التوسيع، العرض، المحاذاة، الأحجام، الألوان، المنطقة الآمنة، والقوالب.
- أُزيلت أحداث النقر المضمنة من القوالب المخصصة واستُبدلت بعناصر آمنة قابلة للصيانة.
- أصبحت المسودة تُحفظ محليًا فورًا وتُستعاد بعد إعادة فتح الصفحة.
- صارت المزامنة تتحقق من `response.ok` وتعرض الفرق بين نجاح السحابة ونجاح الاتصال المحلي بدل إعلان نجاح غير مؤكد.
- أضيف فك غلاف استجابة السحابة (`data`) عند السحب، مع رسائل حالة واضحة داخل الواجهة.
- أُصلح `sw.js` بالكامل وسُجّل Service Worker من التطبيق، مع تحديث هوية وألوان `manifest.json`.
- أُصلح البحث القرآني ليتعامل مع البنية الفعلية المسطحة للملف (`quran[]`) ومع البنية المجمعة القديمة إن وجدت.
- أُعيد تنسيق نتائج القرآن وقائمة المواقع لتناسب الهوية الفاتحة، مع إزالة التنسيق الداكن المكتوب داخل JavaScript.

### التحقق المنفذ

- `node --check pwa-web/app.js` — ناجح.
- `node --check pwa-web/sw.js` — ناجح.
- `node --test pwa-web/tests/ui-contract.test.mjs` — **8/8 ناجحة**.
- اختبار متصفح فعلي بعرض سطح المكتب: لا يوجد overflow، وظهرت 7 بلاطات و7 طبقات.
- اختبار هاتف بواجهة `390×844`: عرض المحتوى يساوي عرض الصفحة، بلا تمرير أفقي، وعرض الساعة 329px.
- اختبار إضافة بلاطة ثم التراجع: `7 → 8 → 7` بنجاح.
- اختبار التنقل بين المصمم والقوالب والإعدادات والمواقع بنجاح.
- اختبار البحث عن «الله»: ظهرت 60 نتيجة، وأولها الفاتحة: 1.
- سجل أخطاء المتصفح أثناء الاختبار: لا توجد أخطاء JavaScript.

### ملفات عُدّلت أو أُضيفت في هذه المهمة

- `pwa-web/index.html`
- `pwa-web/style.css`
- `pwa-web/app.js`
- `pwa-web/sw.js`
- `pwa-web/manifest.json`
- `pwa-web/tests/ui-contract.test.mjs` (جديد)
- `HANDOFF.md`

### ملاحظات فنية وتصميمية للمراجعة القادمة

#### الموقع

- يوجد نظامان قديمان لبيانات القوالب داخل `app.js` (`builtInPresets` و`WEB_PRESETS`). الواجهة الجديدة تستخدم سطحًا موحدًا، لكن توحيد البيانات نفسها في مصدر واحد سيكون تحسينًا معماريًا لاحقًا.
- المزامنة التلقائية ما زالت تحاول السحابة والشبكة المحلية بعد التعديل مع حفظ محلي فوري؛ يمكن لاحقًا إضافة اختيار صريح: «محلي فقط / تلقائي / يدوي» لتقليل محاولات الشبكة.
- القيم الحية في محاكي الويب تقريبية باستثناء الوقت والتاريخ؛ التطابق التام يحتاج عقد بيانات معاينة مشتركًا بين الويب والساعة.
- القوالب القديمة لا تحمل دائمًا كل خصائص الخط/الأيقونة؛ الكود يكمّل القيم الناقصة، لكن الأفضل ترحيلها إلى schema موحد بإصدار واضح.
- قائمة المواقع تعرض كل المواقع العشرة وتربط بالخريطة، لكن تغيير الموقع النشط من القائمة نفسها يحتاج إجراء اختيار صريح في مرحلة لاحقة.

#### الساعة

- `QuranWatchFaceRenderer.kt` يستخدم أحجام خطوط ثابتة حسب عرض البلاطة، ولا يطبق `slot.fontSize` ومواضع النص/الأيقونة القادمة من محرر الويب بصورة كاملة؛ لذلك المعاينة والساعة لن تتطابقا 1:1 بعد.
- التاريخ الهجري في واجهة الساعة مكتوب حاليًا كنص ثابت «١٨ صفر ١٤٤٨ هـ».
- بلاطة متابعة القرآن تعرض «المصحف / موضع القراءة» ولا تقرأ السورة والآية الفعليتين بعد.
- بلاطتا البطارية والطقس تعرضان أيقونة/عنوانًا عامًا داخل renderer بدل القيمة الحية، رغم وجود `WeatherHelper` في المشروع.
- قائمة الإجراءات تعرّف نقرات لبعض البلاطات بقيمة فارغة، ولذلك يلزم لاحقًا ربط `date_big` و`battery` و`weather` بسلوك أو شاشة مناسبة.
- هناك مساران للرسم في المشروع (واجهة Compose الرئيسية وWatch Face Renderer) ويجب تحديد أيهما المصدر النهائي للواجهة قبل توحيد القياسات والتفاعل.

### ما بقي خارج نطاق هذا التنفيذ

1. رفع النسخة الجديدة إلى Vercel؛ لم يتم النشر لأن ذلك إجراء خارجي يحتاج موافقة/جلسة المستخدم.
2. اختبار المزامنة مع ساعة فعلية متصلة؛ الاختبار الحالي تحقق من سلوك الواجهة ورسائل الفشل/النجاح، لا من جهاز بعيد.
3. تطبيق ملاحظات الساعة أعلاه في Kotlin؛ لم تُغيّر ملفات الساعة في هذه المهمة لأن الطلب الحالي كان تغيير هوية صفحة الويب وجمع الملاحظات.

### ملاحظة Git مهمة للوكيل التالي

- جذر Git المكتشف هو `/Users/mohamad` وليس مجلد المشروع نفسه، ولذلك يظهر `HANDOFF.md` ومجلد `pwa-web/` كاملًا كملفات غير متتبعة (`??`). لم يُنفّذ commit أو push، ولم تُلمس تغييرات المستخدم الأخرى.

---

## 🧩 تحسين محرر الساعة — سجل حي (31 أغسطس 2026)

### طلب المستخدم

- إبقاء معاينة الساعة ظاهرة أثناء التمرير داخل إعدادات المصمم.
- تحويل البلاطات إلى زوايا قائمة مع فواصل دقيقة.
- دعم صف رابع ثم خامس، وصف رفيع، وهوامش رفيعة على أطراف الساعة.
- جعل جميع التغييرات تظهر فورًا، وإصلاح السحب والتحجيم للنصوص والأيقونات والبلاطات.

### المنفذ حتى الآن

- أضيف تخطيط مرن حتى خمسة صفوف عبر `MAX_EDITOR_ROWS = 5`.
- أضيفت أوزان للصفوف: يمكن جعل صف العنصر المختار رفيعًا أو استعادة ارتفاعه.
- أضيفت هوامش داخلية قابلة للتبديل من أعلى/أسفل/يمين/يسار؛ تظهر كمساحة محجوزة في المحاكي.
- حُوّل المصمم إلى تخطيط حر بعد تحريك البلاطة، مع حفظ الموضع والحجم داخل حدود الساعة.
- أضيفت مقابض أربع لتغيير حجم البلاطة مع حفظ نسبة العرض إلى الارتفاع، ومقابض منفصلة لتحجيم النص والأيقونة.
- ثُبّتت معاينة الساعة في عمودها أثناء تمرير خصائص المصمم.
- تحولت البلاطات إلى مستطيلات بزوايا قائمة مع فجوة 3px كي تتصل بصريًا من دون تداخل.
- أُضيفت اختبارات عقد لهذه الوظائف؛ يلزم تشغيل الاختبار البصري والوظيفي قبل اعتبار المرحلة مكتملة.

### التحقق المنفذ

- `node --check pwa-web/app.js` — ناجح.
- اختبارات عقد الواجهة أصبحت **9/9 ناجحة**.
- اختبار فعلي: أضيف صفان من الواجهة وظهرت الصفوف 4 و5 (`7 → 9` بلاطات).
- اختبار فعلي: تصغير الصف الخامس من 20% إلى نحو 6%، وإظهار هامش علوي محجوز بنجاح.
- اختبار فعلي: سحب نص داخل البلاطة غيّر موضعه فورًا من 50% إلى 65%/80%.
- اختبار فعلي: تحجيم بلاطة من مقبض الزاوية غيّر العرض والارتفاع مع حفظ النسبة.
- اختبار فعلي: بعد تمرير 620px بقيت المعاينة مثبتة عند `top: 88px` بينما تحركت لوحة الخصائص.
- اختبار هاتف `390×844`: لا تمرير أفقي، المعاينة مثبتة عند `top: 58px`، وظهرت مقابض التحجيم.
- سجل أخطاء JavaScript في المتصفح: فارغ.

### ملاحظة تنفيذ

- تأخرت المعاينة الثابتة في نسخة أولية لأن العنصر المثبت كان مقيدًا بارتفاع صف الشبكة. حُلّت بوضع عنصر داخلي `canvas-sticky` داخل عمود طويل مخصص للمصمم؛ لا تُستبدل هذه البنية بعنصر sticky مباشر على العمود.

---

## 🧠 شبكة البلاطات الذكية وإثراء الإعدادات — سجل حي (31 أغسطس 2026)

### طلب المستخدم

- عند تصغير عرض بلاطة، تكبر جاراتها فورًا كي يكتمل الصف، وعند تعديل الارتفاع تتغير كل بلاطات الصف نفسه ويُعاد توزيع الصفوف التالية.
- منع النص من تجاوز البلاطات، وترتيب تلقائي متجدد يعتمد على حجم/أهمية المحتوى.
- تنسيق بلاطة آخر قراءة بصيغة `الكهف 18` ثم بداية الآية في سطرين.
- إجراءات أكثر تخصّصًا لكل ميزة، خصوصًا المواقع الأخيرة والملاحة.
- إظهار خطوط وألوان القارئ الفعلية في الساعة، وإعدادات تنبيه أدق.

### المنفذ

- أضيفت `rebalanceRowWidths()` إلى محرر الويب: سحب مقبض بلاطة شبكية أفقيًا يغيّر عرضها ويعيد توزيع الوحدات المتبقية على بقية بلاطات الصف؛ لا يترك فراغًا.
- التعديل الرأسي لمقبض بلاطة شبكية يغير وزن الصف بالكامل، فتأخذ بلاطات الصف الارتفاع نفسه وتتمدد/تنكمش الصفوف الأخرى تلقائيًا ضمن الشاشة.
- يحتفظ التراجع/الإعادة الآن بأوزان الصفوف وهوامش الشاشة، لا بالبلاطات وحدها.
- زر «ترتيب تلقائي» صار يولّد ثلاثة أنماط مدروسة بالتناوب ويعيد ترتيب البلاطات بحسب الأولوية (الساعة، القراءة، الصلاة…)، بدل أن ينفذ إعادة رسم فقط.
- منعت CSS خروج النص أو الأيقونات من البلاطات. النص العام يلتف ويقتصر على سطرين؛ وبلاطة القراءة لها قالب خاص: `الكهف 18` ثم `وَتَحْسَبُهُمْ أَيْقَاظًا وَهُمْ رُقُودٌ` في المساحة الباقية.
- أضيف كتالوج أفعال سياقي في المحرر: القراءة (متابعة/آية تالية/علامة)، المواقع (الأخيرة/النشط/ملاحة/حفظ الموقع)، التسبيح، القبلة، الطقس، البطارية، المجلدات، المواقيت، والإعدادات.
- صفحة المواقع تعرض الآن «آخر المواقع»، وزر تفعيل لكل موقع، ورابط الملاحة؛ التفعيل يحدث الموقع النشط وقائمة الاستخدام الأخير.
- صفحة الإعدادات تطابق خيارات القارئ الموجودة في تطبيق الساعة: الخطوط `default/uthmani/kufi/sansserif/serif`، ألوان الآية والخلفية والنص المدعومة، وحجم 12–32.
- أضيفت تنبيهات مستقلة للفجر/الظهر/العصر/المغرب/العشاء مع خيارات الاهتزاز وملء الشاشة. تُرسل في حقل `prayerReminders`.
- عُدّل `LocalSyncServer.kt` ليتبادل ألوان الآية، حالة التنبيهات وطريقة الحساب، ويحفظ كائن `prayerReminders` عند المزامنة المحلية.

### التحقق

- `node --check pwa-web/app.js` — ناجح.
- `node --test pwa-web/tests/ui-contract.test.mjs` — **11/11 ناجحة**.
- تحقّق HTTP محليًا من تشغيل الصفحة من `pwa-web/` — ناجح.
- محاولة `./gradlew :app:compileDebugKotlin` لم تبدأ لأن بيئة العمل الحالية لا تحتوي Java Runtime؛ لا توجد نتيجة بناء Android بعد هذا التعديل.

### ما بقي

1. `PrayerAlarmScheduler` لا يقرأ بعد تفاصيل `prayerReminders` الجديدة ليجدول كل صلاة بصورة مستقلة؛ البيانات تُحفظ وتُزامن الآن، لكن ربطها الفعلي بجدولة Android هو الخطوة التالية.
2. أفعال البلاطات السياقية الجديدة جاهزة في إعداد التصميم والمزامنة، لكنها تحتاج ربطًا مطابقًا في معالج نقرات الساعة (`Compose`/واجهة الساعة) كي تنفذ على الجهاز.
3. يفضّل اختبار المزامنة مع ساعة فعلية ثم التحقق من التنفيذ البصري للخطوط والتنبيهات على شاشة الساعة.

---

## 🎛️ تصحيح ترتيب البلاطات وزر الترتيب الذكي — سجل حي (31 أغسطس 2026)

### طلب المستخدم

- جعل الترتيب التلقائي يختار الألوان والأحجام بحسب حجم المحتوى، ومنح النصوص الطويلة/المواقيت مساحة أكبر.
- نقل الزر إلى أعلى معاينة الساعة تحت أدوات النسخ/اللصق/الكل/الحذف، وتقليل مساحة الأدوات إلى صف أو صفين.
- منع تراكب البلاطات أو ترك خلايا فارغة بعد تكبير/تصغير أي منها.

### المنفذ

- أصبح زر **«ترتيب تلقائي ذكي»** داخل شريط معاينة الساعة، فوق الساعة مباشرة. الشريط منظّم في صفين مضغوطين: تراجع/إعادة والمنطقة الآمنة، ثم النسخ/اللصق/الكل/الحذف والزر الذكي.
- الترتيب الذكي يدوّر بين ثلاث كثافات، ويعيد اختيار اللون وحجم الخط وفق نوع البلاطة وطول النص. بلاطة بداية الآية و`prayer_strip_5` والمواقيت الكاملة تأخذ صفًا كاملًا؛ الساعة والعد التنازلي نصف صف؛ والمحتوى الأقصر يصغّر بصورة مدروسة.
- ألغي التخطيط الحر المتداخل من شبكة المصمم: السحب يعيد البلاطة إلى صف شبكي، والتحجيم يعيد توزيع وحدات الصف. `validateAndPackGrid()` يسوّي كل صف إلى 12 وحدة بصورة حتمية، فلا تراكب ولا فراغات.
- رُحّلت المسودات القديمة التي كانت تحتوي `manualLayout` إلى الشبكة تلقائيًا في أول تحقق، لتختفي أي طبقات متداخلة محفوظة سابقًا.

### التحقق

- `node --check pwa-web/app.js` — ناجح.
- `node --test pwa-web/tests/ui-contract.test.mjs` — **12/12 ناجحة**.

---

## ✦ نظام مظهر الساعة ومكتبة الزخارف والأيقونات — سجل حي (31 أغسطس 2026)

### طلب المستخدم

- إضافة خيارات جماعية لشكل البلاطات: مربع متصل، مربع بفواصل، دائري، وبيضاوي.
- دعم مكتبة واسعة من الخلفيات الزخرفية الإسلامية والإقليمية، ومكتبة أيقونات عامة ومتخصصة لا تقل عن مائة رمز.

### المنفذ

- أضيف كائن `tileConfig.appearance` المُزامَن مع التصميم: `tileShape` و`pattern` و`iconPalette`.
- تظهر في **الإعدادات ← واجهة الساعة** أدوات مباشرة لتغيير شكل البلاطات ولوحة الألوان والخلفية؛ كل تغيير يعيد رسم المحاكي ويحفظ المسودة.
- أشكال البلاطات المدعومة: `square-connected` (من دون فواصل)، `square-gapped`، `circle`، `oval`.
- أضيفت 15 خلفية: سادة، نجمة ثمانية، هندسية، أرابيسك، مغربي، أندلسي، دمشقي، مصري، عثماني، فارسي، آسيوي إسلامي، إندونيسي، هندي إسلامي، إفريقي إسلامي، وعالمي هندسي. جميعها CSS خفيفة تُرسل كاختيار، وليست صورًا ثقيلة.
- أضيفت `expandedIconLibrary` فيها **105 أيقونات إضافية** (أي أكثر من 120 في المكتبة الكاملة بعد المكتبة السابقة): القرآن والتلاوة والقبلة والكعبة والمئذنة والمسبحة والأذان والصلوات والمواقيت والحج، ثم الطقس والموقع والتنقل والملفات والوسائط والاتصال والأمان والتحكم والزخارف.

### ملاحظة صادقة عن جودة الأيقونات

- المكتبة الجديدة حالياً رموز محلية ملوّنة لتصبح الوظيفة متاحة فوراً في الويب والساعة. صور التصميم التي وُلّدت سابقًا هي مرجع بصري، وليست ملفات أيقونات فردية قابلة للاستخراج بدقة.
- للوصول إلى أيقونات مطابقة تمامًا لذلك الأسلوب، الخطوة التالية هي إنشاء حزمة SVG مخصّصة موحدة (خط/حجم/حدود/ألوان) ثم ربطها بالمُعرّفات الحالية، بدل استخدام الرموز المحلية.

### التحقق

- عدّ مكتبة الإضافات: **105** عناصر.
- `node --check pwa-web/app.js` — ناجح.
- `node --test pwa-web/tests/ui-contract.test.mjs` — **13/13 ناجحة**.

---

## 🛟 نقطة استعادة قبل الإكمال الشامل (31 أغسطس 2026، 19:04)

- أنشئ الأرشيف المحلي: `.restore-points/reloj-before-full-completion-20260831-1904.tar.gz`.
- بصمة SHA-256: `9f6c15c5a575f63953fc8547abeca68a08a851a2c7ace0443ef1fd885afee2c4`.
- تفاصيل الاستعادة في `.restore-points/README.md`.
- لا يشمل الأرشيف بيانات Vercel المحلية؛ يشمل مصدر Android، `pwa-web`، وملف التسليم كما كانت قبل متابعة مرحلة الإكمال.

---

## 🚀 مرحلة الإكمال الشامل والنشر (31 أغسطس 2026)

### ما نُفّذ

- أضيف `WatchAppearance` إلى نموذج Android وملف `TileConfig`؛ يستقبل ويحفظ `tileShape` و`pattern` و`iconPalette` المرسلة من محرر الويب.
- طبّقت واجهة Compose للأشكال الأربعة والألوان العامة، وطُبّق لون الخلفية المناسب للنمط.
- أصلح `QuranWatchFaceRenderer` ليرسم البلاطات حسب مواضع وأحجام محرر الويب الفعلية بدل تقسيمه القديم إلى ثلاثة صفوف ثابتة، ويطبق الشكل والألوان المرسلة.
- وسّعت مسارات أفعال البلاطات في Android للقراءة، المواقع، القبلة، التسبيح، المواقيت، الطقس، البطارية والإعدادات.
- صار تحويل تنبيهات الويب المفردة إلى قوائم تنبيهات Android صحيحًا عند المزامنة، ويستخدم المستقبل فعليًا خيارات الاهتزاز وملء الشاشة.
- النمط الدائري صار **دائرة فعلية** في محاكي الويب وCompose وWatch Face: يصغر إلى مربع متساوي الأبعاد داخل المساحة المحجوزة بدل أن يتحول إلى بيضاوي.

### النشر

- نشرت نسخة الإنتاج على Vercel: https://quran-watch8-hub.vercel.app
- نشر أخير جاهز: `dpl_DyZoSb5fMStS4KpWa8MH8TCozT2n`.

### التحقق والحالة

- `node --check pwa-web/app.js` — ناجح.
- `node --test pwa-web/tests/ui-contract.test.mjs` — **13/13 ناجحة** بعد إضافة النمط الدائري الفعلي.
- تعذر بناء Android لأن بيئة العمل لا تحتوي Java Runtime أو Java المرفقة بـAndroid Studio. لم يُثبت JDK جديد كي لا أغيّر بيئة الجهاز دون حاجة صريحة؛ يلزم بناء لاحق بمجرد توفر Java.

---

## 🌀 إصلاح الترتيب الذكي للأشكال (31 أغسطس 2026)

- كان الترتيب الذكي السابق يضع البلاطات وفق شبكة مستطيلة ثم يقصّها كدوائر؛ لذلك بدا عشوائيًا وترك مساحات غير ذات معنى.
- صار لنمط `circle` مسار مستقل باسم `applyCircularAutoLayout()`:
  - يضع بلاطة القبلة في الوسط متى وجدت (وإلا أول بلاطة مهمة).
  - يرتب بقية البلاطات على حلقة شعاعية متساوية الزوايا والمسافات.
  - يصغّر العناصر تلقائيًا عندما يزيد عددها، فلا تتراكب الدوائر.
- المربع والبيضاوي يواصلان استخدام ترتيب شبكي/كبسولي منفصل، ولا يمران بخوارزمية الدائرة.
- طبّقت نفس توزيع الحلقة في Compose و`QuranWatchFaceRenderer` حتى تتوافق الساعة مع معاينة الويب.
- التحقق: `node --check` و**13/13** اختبار واجهة ناجحة.
