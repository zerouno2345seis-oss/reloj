# ملخص متجدد — تطبيق Quran Watch 8 ولوحة الويب PWA

## للبدء في جلسة جديدة
اقرأ هذا الملف فقط للسياق، ولا تستكشف بقية المشروع أو أي سجل محادثة سابق إلا إذا احتجت لذلك. تابع من "الخطوة التالية" أدناه.

## الحالة الحالية
- **حزمة إصلاح الواجهة الأخيرة (غير ملتزمة بعد)**: أُصلحت هوامش الشاشة الدائرية (`WatchSafeInsets` جديد: هامش 12٪ + `listContentPadding` موحّد عبر الإعدادات/المواقع/المواقيت/القرآن/القوالب)، تراكب عنوان الإعدادات (صار «القوالب الجاهزة» سطر واحد 15sp بلا سطر فرعي)، قصّ أسماء التطبيقات (سطران 8sp عرض 72dp) والموقع والمواقيت، وتوحّدت أيقونات المواقع والقرآن (متجهات `WatchIcons.Car/Star/Mosque` + `LocationPin`/`MicRecording`/`Bookmark` بدل الإيموجي). الحفظ السريع للمواقع صار زرين فقط: «مهم» و«موقف سريع». أزيل عنوان «🕘 الأخيرة» من الدرج، وأزيل ⭐ من «المثبتة»، ويُختصر اسم تطبيق النظام «التطبيقات الأخيرة» إلى «التطبيقات» (`compactAppName`). رُفع سطر الصلاة في «محراب الفجر» إلى 18sp، وصار عدّاد الصلاة عربيًا «س/د» بدل «h/m» (`PrayerTimesHelper.formatCountdown`). بلاطة «متابعة القرآن» + تعقيد `QURAN_RESUME` صارا يعرضان اسم السورة ورقم الآية ونصّها (`formatQuranReadingLine`، سطران). أُزيلت أرقام المحيط المرسومة برمجيًا من 3 واجهات + المعاينات (الحاشية الدوّارة الفيزيائية تحملها)، و«فلكي محيطي (أرقام)» صار «فلكي هادئ». خطوط جدول المواقيت في البلاطات صارت Monospace مضغوطة. أضيفت أيقونة التطبيق الجديدة لكل كثافات launcher.
- **آخر تحقق محلي (2026-09-03)**: اختبارات الويب **52/52**، وحدات Android **12/12** (`PrayerTimesHelperTest` جديد + 10 سابقة)، `assembleDebug` ناجح، Android Lint **0 خطأ / 0 fatal**. APK: `app/build/outputs/apk/debug/app-debug.apk`، **73,528,306 بايت** (البناء up-to-date — تغييرات الوكيل الآخر كلها مُصرَّفة فيه؛ أُعيد التصريف بـ`--rerun-tasks` بنجاح للتأكد).
- **حالة الجهاز (2026-09-03 ~12:00)**: ثُبّت `app-debug.apk` على `SM_L500` عبر `adb -t 161` → `Success`. تحقّق بصري: (1) الدرج يُفتح بسحبة من الخُمس السفلي — يظهر «التطبيقات» بلا «الأخيرة»، «المثبتة» بلا ⭐، مبدّل شبكة/⌂. (2) صفحة البلاطات: عدّاد «الظهر 54 د» عربي، بلاطة القرآن تعرض «إبراهيم · ١٠» + نصّ الآية ملفوفًا، بلا خروج نص. (3) المواقع: زرّان «★ مهم» و«🚗 موقف سريع» بأيقونات متجهة + `LocationPin` للسطر — لا زر «مسجد». (4) الإعدادات: «القوالب الجاهزة» سطر واحد بلا تراكب، هوامش آمنة. (5) logcat نظيف — لا FATAL ولا ANR. **لم يُتحقّق بصريًا بعد**: واجهة FAJR_MIHRAB نفسها (تكبير سطر الفجر 18sp مؤكَّد في الكود فقط)، وأيقونة التطبيق في مشغّل الساعة.
- **معمارية**: الطبقة الأولى `WatchFaceHomeScreen.kt` (15 واجهة؛ 6 جديدة في `ui/screens/watchfaces/`)، الطبقة الثانية `HomeScreen.kt` (بلاطات 12 وحدة)، ووجه نظام `QuranWatchFaceRenderer.kt` (بدائي).
- **توحيد اللغة البصرية** (خطة من 5): 1 البلاطات ✅ مُعايَنة على الساعة · 2 الواجهات التسع ✅ لا تحتاج شيئًا (داكنة أصلًا، chrome تعقيدات مشترك هادئ، والعنصر الساطع الوحيد هو حلقة تنبيه الصلاة الوظيفية) · 3 معاينات الويب للـ15 ⬜ · 4 نقاط التنقّل ✅ (لم تُعايَن بعد) · 5 «فسيفساء المؤمن» ✅ صار 3 أشرطة بدل 4.
- **الويب**: «نظرة عامة» صادقة الآن (بيانات حقيقية بدل مخترعة). درج التطبيقات فوري + «الأخيرة» + زر ⌂.
- **المزامنة**: تعمل (Vercel KV + سحب عند onResume). حمولة `syncAll` لا ترسل `locations`/`bookmarks`.
- **الاختبارات**: 47 عقد ويب + 10 وحدة JVM، خضراء. البناء نظيف.
- **مستند قرار التصميم**: https://claude.ai/code/artifact/0354fc6b-5869-4de5-a5e4-978d6d33efd3

## القرارات المهمة
- **المخزن السحابي: Vercel KV** — لا يخمل (خلافًا لـ Supabase المجاني الذي يتوقف بعد 7 أيام خمول)، ومدمج في منصة النشر الحالية.
- **قناة LAN تُحذف** — الاتصال المباشر بـ `http://<ip>:41331` يحجبه المتصفح (mixed content) لأن اللوحة تُخدَّم عبر HTTPS، فهي كود ميت يُبطئ كل مزامنة بمهلة فاشلة.
- **الطبقة الأولى: توضيح فقط** — يُعاد تسمية القسم إلى "لوحة التطبيق" بدل ربط `QuranWatchFaceRenderer` بـ `WatchFaceConfig` (الراندرر الحقيقي يتجاهلها تمامًا حاليًا).
- **الشبكة تبقى 12 وحدة** — التحجيم يقفز بمقدار وحدة لأن الساعة تستقبل `colSpan` صحيحًا؛ التحجيم الحر يتطلب تغيير نموذج البيانات المتزامن.
- **البناء يحتاج JDK صريحًا**: `JAVA_HOME=/opt/homebrew/opt/openjdk@17 ./gradlew assembleDebug` — لأن `/Library/Java/JavaVirtualMachines/` فارغ فلا يجده `java_home`.
- **الاتصال بالساعة**: الساعة على `192.168.1.226` لكن التصحيح اللاسلكي يستعمل منفذًا عشوائيًا لا 5555؛ الاتصال يتم تلقائيًا عبر mDNS (`adb devices` بعد تفعيل التصحيح). تسقط الوصلة عند إطفاء الشاشة.

## تشخيص استنزاف البطارية (2026-09-03، مبني على قياس فعلي من `SM_L500`)
مقسوم إلى سببين منفصلين — الأول ليس خطأ التطبيق:

**أ. إعدادات الجهاز (تفسّر معظم الاستنزاف المرصود):**
- `settings get system screen_off_timeout` = **600000** (10 دقائق بدل 15 ثانية المعتادة على Wear). كل نظرة تُبقي شاشة OLED مضاءة 10 دقائق.
- `settings get global stay_on_while_plugged_in` = **15** (كل المصادر) — خيار المطوّر «ابقَ مستيقظًا» مفعّل بالكامل.
- النتيجة في `dumpsys batterystats`: «Screen on: 100.0% · Screen brightnesses: bright 100.0% · Time on battery screen off: 0ms · Wrist up: 0x». الشاشة لم تنطفئ ولا مرة، وسحب الشاشة = **كل** الاستنزاف (5.68 mAh من أصل 5.68 mAh).

**ب. معمارية التطبيق (أخطاء حقيقية في الكود):**
1. **`MainActivity` تعلن `android.intent.category.HOME`** — و`cmd package resolve-activity` يؤكد أن `com.quran.watch8.MainActivity` هي شاشة HOME الفعلية للساعة. النتيجة: مسار Wear OS منخفض الطاقة للواجهات (رسم مُفرَّغ للعتاد عند 1Hz في ambient) **يُتجاوَز كليًا**، ولا يوجد في `MainActivity` أي `AmbientLifecycleObserver`/`onEnterAmbient`. الواجهة التي يراها المستخدم هي Compose Activity كاملة بلا حالة منخفضة الطاقة. **هذا السبب الجذري رقم 1.**
2. **حلقات `while(true) { delay() }` داخل `LaunchedEffect` مربوطة بالتركيب لا بدورة الحياة** — لا وجود لـ`repeatOnLifecycle` في المشروع كله. ولأن الـActivity هي HOME فهي عمليًا لا تُدمَّر، فتستمر الحلقات والشاشة مطفأة: `WatchFaceHomeScreen.kt:107` (ثانية واحدة على واجهتَي الكرونوغراف = 60 إيقاظ/دقيقة للأبد)، `HomeScreen.kt:264` (دقيقة)، و**`HomeScreen.kt:277` — `delay(4000L); liveStep++` تعيد تركيب شبكة الـ12 بلاطة كل 4 ثوانٍ إلى الأبد** (900 إعادة تركيب/ساعة).
3. **`MainViewModel.kt:149`**: `while(true){ delay(30 دقيقة); refreshWeather() }` في `viewModelScope` — جلب شبكة دوري يُبقي العملية مقيمة. مكانه `WorkManager`.
4. **`LocalSyncServer` ما زال يشغّل `ServerSocket` على المنفذ 41331** (`accept()` حلقة لانهائية، تبدأ في `onCreate` ولا تتوقف أبدًا) — وهذه بالضبط «قناة LAN» التي قرّر هذا الملف حذفها كـ«كود ميت» لأن المتصفح يحجبها (mixed content). خيط + مقبس إصغاء دائمان لميزة لا تعمل.
5. **`NewWatchFaces.kt:250`**: واجهة القبلة تسجّل `TYPE_ROTATION_VECTOR` بـ`SENSOR_DELAY_UI` (~16Hz) — مستشعر مدمج (جيرو+تسارع+مغناطيس) وهو من أغلى المستشعرات. التسجيل يُلغى في `onDispose` بشكل صحيح، لكن إن كانت واجهة القبلة هي المختارة فهو مسجَّل باستمرار ما دام تركيب الشاشة حيًّا — بما في ذلك والشاشة مطفأة.

**حالة التنفيذ (2026-09-03)**: نُفِّذت بنود الكود ٢–٥ أدناه. **البند ١ (إعداد الجهاز) لم يُنفَّذ** — يحتاج قرار المستخدم.

- ⬜ **١. `screen_off_timeout` = 600000** — أكبر أثر منفرد وبلا كود. الأمر: `adb shell settings put system screen_off_timeout 20000`. لم أغيّره لأنه إعداد شخصي على جهاز المستخدم. (و`stay_on_while_plugged_in=15` خيار مطوّر يُطفأ من إعدادات المطوّر.)
- ✅ **٢. تصحيح لتوصية سابقة خاطئة** — كنت اقترحت «إضافة دعم ambient» وهذا **يزيد** الاستهلاك لا يقلّله: `AmbientLifecycleObserver` يجعل الـActivity always-on فتبقى RESUMED والشاشة مضاءة خافتة بدل أن تُوقَف عند إطفاء الشاشة. `category.HOME` تُرك كما هو (التطبيق مشغّل الساعة عمدًا)، والضرر الحقيقي منه — أن التركيب لا يُدمَّر أبدًا فتعمل المؤقتات للأبد — عولج بالبند ٣.
- ✅ **٣. `RepeatOnResumed`** (`ui/components/LifecycleEffects.kt` جديد) يلفّ `repeatOnLifecycle(RESUMED)`. طُبِّق على: مؤقّت `WatchFaceHomeScreen` (الثانية/الدقيقة)، مؤقّتَي `HomeScreen` (الدقيقة + `liveStep` كل 4 ث)، ومستشعر البوصلة في `NewWatchFaces` و`QiblaCompassScreen` (مع `awaitCancellation` + `finally` لإلغاء التسجيل). المستشعر نزل أيضًا من `SENSOR_DELAY_UI` (~16Hz) إلى `SENSOR_DELAY_NORMAL` (~5Hz).
- ✅ **٤. حُذف `ServerSocket`** كليًا من `LocalSyncServer` (‑143 سطرًا: `start`/`stop`/`handleClient`/`sendResponse`/`FIXED_PORT`)، وحُذف نداء `LocalSyncServer.start` من `MainActivity.onCreate` (سحب الإقلاع كان يكرّر سحب `onResume`). قناة HTTPS باقية كما هي.
- ✅ **٥. الطقس بلا مؤقّت خلفي**: حُذفت `while(true){delay(30min)}` من `MainViewModel.init`. بديلها `refreshWeatherIfStale()` تُنادى من `MainActivity.onResume` ولا تجلب إلا إذا تجاوز عمر الكاش `WEATHER_MAX_AGE_MS` (30 د). لم تُضَف تبعية WorkManager — صفر إيقاظ خلفي أفضل من إيقاظ مجدول على ساعة.

## قائمة العمل الحيّة
`PLAN.md` في الجذر هو القائمة الحيّة المُحدَّثة بعد كل بند. الأقسام أ/ب/ج/ز/ح كلها `[x]` الآن (ز = الخطوة 2، ح = الخطوة 3 من توحيد اللغة البصرية).

## قياس البطارية — أُغلق كغير حاسم (2026-09-03)
A/B على `SM_L500` عبر `/data/local/tmp/batt.sh` (قراءتا `/proc/<pid>/stat` حول نافذة زمن‑حائط، شاشة مطفأة).

| قياس | البناء | عمر العملية | نافذة | CPU-s/min (شاشة مطفأة) |
|---|---|---|---|---|
| before2 | قديم | ~3 س | 752s | **0.077** |
| after  | جديد | 44 ث بارد | 812s | 0.568 (مرفوض) |
| after2 | جديد | ~22 د | 1091s | 0.212 |
| after3 | جديد | ~48 د | 625s | 0.345 |

**النتيجة: المنهج أخشن من أن يحسم.** الأرقام لا تنحدر بثبات (0.57→0.21→0.35) لأن نوافذ صيانة Doze وGC تسقط عشوائيًا داخل نافذة ~10 د، وN=1 لكل حالة. استهلاك التطبيق خلف شاشة مطفأة صغير أصلًا (<1٪ دورة تشغيل) ومغمور بضجيج Doze. لا يمكن إثبات تحسّن ولا انحدار من هذه البيانات.

**استنتاج مفاهيمي مهم**: أثناء Doze الحقيقي يُجمّد نظام التشغيل كل `delay()` في الكوروتينات — فتغييرات `RepeatOnResumed` **لا تنفع أثناء Doze** (وهو معظم وقت الشاشة المطفأة). قيمتها في نافذة «شاشة مطفأة قبل Doze» القصيرة المتكررة، وفي منع إبقاء العملية دافئة. حذف الـ`ServerSocket` حقيقي لكنه ضئيل. الحزمة **نظافة معمارية صحيحة لكنها لم تكن لتحرّك المؤشر بشكل ملموس** — التشخيص الأصلي قال إن السبب الجذري هو `screen_off_timeout=600000` (الشاشة لا تنطفئ)، ولا يعالجه أي تغيير كود.

**ما تأكّد بلا قياس**: (1) منفذ 41331 لم يعد يُصغي — الخيط + المقبس الدائمان زالا. (2) لا wakelock يحمله التطبيق (`dumpsys power` → `Wake Locks: size=0`) فهو لا يمنع نوم الجهاز. (3) البناء الجديد مثبّت، بلا أعطال، 57/57 + 12/12.

**التوصية**: (أ) أوقف القياس المجهري — commit الحزمة كما هي. (ب) المكسب الملموس الوحيد هو `adb shell settings put system screen_off_timeout 20000` — قرار المستخدم. (ج) لبيانات بطارية حقيقية: شغّل البناء الجديد يومًا عاديًا واقرأ إعدادات ← البطارية.


## الخطوة التالية
**أولًا: إغلاق حزمة الواجهة الحالية على الساعة** (الجهاز غير موصول الآن): فعّل التصحيح اللاسلكي واترك صفحته مفتوحة، اكتشف منفذ mDNS الجديد، ثم ثبّت `app-debug.apk` الحالي. تحقّق بصريًا من أيقونة التطبيق، «مهم/موقف سريع»، عنوان الإعدادات، «التطبيقات» بلا «الأخيرة»، وضوح سطر الفجر، وعدم خروج النصوص. أعد اختبار أن السحب من المنتصف لا يفتح الدرج وأن السحب من الخُمس السفلي يفتحه، وافحص logcat للأعطال وANR. بعد نجاح ذلك حدّث هذه الفقرة بنتيجة الجولة الفعلية.

**ثم فحص المزامنة السابق الذي ما زال مطلوبًا**:
1. **الطقس** — افتح التطبيق، انتظر ~20ث، تأكّد أن بلاطة الطقس تعرض حرارة لا «—°»؛ ثم أعد التشغيل بلا شبكة للتأكد أنها تعرض آخر قيمة مخزّنة.
2. **`customLabel`** — من مصمم الويب ضع اسمًا مستعارًا (مثلًا `🕌 {name} — {time}` على بلاطة الصلاة، و`ضبط` على الإعدادات، وإيموجي مخصّصًا على الطقس)، احفظ وأرسل، ثم تحقّق أنها تظهر على الساعة كما في المعاينة.
بعدها: قرار زوايا الواجهات الـ15 (`slotOverrides`)؛ و`app.js` ≈ 4000 سطر؛ ولا اختبارات بصرية.

**ثغرات حزمة الوكيل الآخر — الحالة**:
- ✅ **المرآة ويب↔ساعة**: أُصلحت في **٤** مواضع لا ٣ (`app.js` سطور المعاينة + `webFaceData().countdown` + `sampleText` للتعقيدات في الواجهات التسع). اختبار عقد جديد **يستخرج الوحدات من `formatCountdown` نفسها** ويقارنها بكل نصوص الويب، فتغيير المنسّق يُفشِل الاختبار حتى تتبعه المعاينة.
- ✅ **الأيقونات الميتة**: حُذفت `Mosque`، **واختبار «كل أيقونة مستخدمة» كشف ٦ أخرى ميتة أصلًا** (`QuranBook`, `PrayerMosque`, `SettingsGear`, `Battery`, `Weather`, `CalendarDate`) — حُذفت كلها (‑390 سطرًا). `WatchIcons` الآن ٨ أيقونات كلها مستعملة.
- ⬜ **العقود القديمة الأربعة ما زالت grep على نص المصدر** — لم أعد كتابتها؛ العقود الجديدة الخمسة سلوكية أو تُجرِّد التعليقات (`codeOnly()`) قبل المطابقة.
- ⬜ **`contentWidthFraction = 0.76f` ثابت** — لم يُمَس.
- ⬜ **اللغة البصرية منقسمة**: الشاشات صارت متجهات `WatchIcons`، لكن **شبكة البلاطات ما زالت إيموجي بالكامل** (`getActionIcon` خريطة إيموجي في `HomeScreen.kt:167`). لا توجد أي صلة بين النظامين.

## سجل الإنجاز (الأحدث أولًا)
- [2026-09-03] **حزمة البطارية + إصلاح المرآة والكود الميت** (غير ملتزم، **لم يُثبَّت على الجهاز بعد**): `RepeatOnResumed` جديدة تلفّ `repeatOnLifecycle(RESUMED)` وتحكم ٥ مواضع كانت تعمل للأبد خلف شاشة مطفأة (٣ مؤقّتات + مستشعرا بوصلة، والمستشعر نزل من 16Hz إلى 5Hz). حُذف `ServerSocket` الميت (‑143 سطرًا) ونداء `LocalSyncServer.start`. حُذف مؤقّت الطقس الخلفي لصالح `refreshWeatherIfStale()` عند `onResume`. أُصلحت مرآة العدّاد في ٤ مواضع بالويب. حُذفت ٧ أيقونات ميتة (‑390 سطرًا). **٥ عقود جديدة**: مرآة العدّاد (تستخرج الوحدات من Kotlin)، «كل أيقونة مستخدمة» (تمسح كل مصادر Kotlin)، «لا مؤقّت خارج RESUMED»، «لا socket»، «لا مؤقّت طقس». الاختبارات: ويب **57/57** · وحدات **12/12** · lint **0 خطأ**. صافي التغيير ‑288 سطرًا.
- [2026-09-03] **مراجعة حزمة إصلاح واجهة وكيل آخر + تثبيت وتحقّق على الجهاز**: راجعت 12 ملف Kotlin معدّلًا + `WatchSafeInsets.kt` جديد + `PrayerTimesHelperTest.kt` جديد + 4 عقود ويب جديدة في `android-contract.test.mjs`. `formatQuranReadingLine` موجود في `WatchFaceCalculations.kt`. البناء نظيف: ويب 52/52، وحدات 12/12، lint 0 خطأ، `assembleDebug` ناجح. ثُبّت على `SM_L500` وتُحقّق بصريًا (لقطات): الدرج «التطبيقات»/«المثبتة» بلا زخارف، عدّاد «54 د» عربي، بلاطة قرآن بنصّ آية ملفوف، زرّا مواقع «مهم»/«موقف سريع» متجهان، عنوان إعدادات سطر واحد، هوامش آمنة، logcat بلا أعطال. المتبقّي بصريًا: واجهة FAJR_MIHRAB وأيقونة المشغّل. غير ملتزم بعد.
- [2026-09-02] **`customLabel` للبلاطات + إيموجي حرّ + إصلاح ⭐**: حقل `customLabel` في `SlotItem` (يُسلسَل ضمن `tilesConfig` فيتزامن تلقائيًا)، و`applyLabelTemplate()` بنفس العناصر النائبة على الطرفين: `{default} {name} {time} {value} {countdown}` — فـ«🕌 {name} — {time}» على بلاطة صلاة تعطي «🕌 المغرب — 18:36»، و«ضبط» تستبدل «الإعدادات». حقلان جديدان في لوحة الخصائص: «اسم مستعار» (في المحتوى) و«أيقونة مخصّصة» (في الخط والأيقونة) مع تلميح بالعناصر النائبة. وأُصلح أن `iconType` غير المعروف كان يعرض ⭐ بدل الإيموجي المكتوب (`getActionIcon`/`getIcon` تُرجعه حرفيًا). اختبار عقد جديد يثبّت المرآة. 48/48 + 10/10.
- [2026-09-02] **عولجت هشاشة الطقس**: كان جلبًا واحدًا عند الإقلاع بلا إعادة محاولة ولا تخزين وبتحليل هشّ. الآن `WeatherHelper` يعيد المحاولة 3 مرات بتباعد، يقرأ بـ`opt*`، ويطلب `timezone=auto`. `WeatherSnapshot` صار يُسلسَل JSON مع `fetchedAt`/`isStale`. و`MainViewModel` يبذر القيمة من كاش DataStore عند الإقلاع (فلا «—°» أثناء الجلب)، ويخزّن كل جلب ناجح، ويعيد الجلب كل 30 دقيقة وعند تغيّر الإحداثيات، **ولا يستبدل قيمة جيدة بـ«غير متوفر» عند فشل الجلب**. 47/47 + 10/10. لم يُختبر على الجهاز بشبكة بعد.
- [2026-09-02] **الفحص الشامل على `SM_L500` — كله سليم**: (1) البلاطات ملتحمة بسيم رفيع، لوحات داكنة مصبوغة بلون كل بلاطة، وتعرض معلوماتها الحيّة (الفجر 06:01 · 72% · 76° · الموقع الحالي · العلامات·العدد · الإعدادات؛ الطقس «—°» لعدم توفّره). (2) نقطتا التنقّل تظهران أسفل الواجهات. (3) «فسيفساء المؤمن» ثلاثة أشرطة موزّعة بلا تراكب. (4) إيماءة الدرج: سحبة من وسط الشاشة لا تفتحه، وسحبة من الحافة السفلى تفتحه (مؤكَّد بـ`dumpsys` + لقطة). (5) التسجيلات مختفية، لا عطل، جدول `voice_notes` خامل. (6) **مزامنة المواقع والإشارات ويب→ساعة مؤكَّدة**: `saved_locations` فيها مكة والمدينة، و`bookmarks` فيها الإشارة الاختبارية (من dump قاعدة بيانات الساعة).
- [2026-09-02] **حُذفت ميزة التسجيلات الصوتية من الويب والتطبيق** + **أُصلحت حساسية إيماءة الدرج**: أُزيلت `VoiceNotesScreen` ومساراها، وإجراء البلاطة وأيقونته، ومدخل `folder_tools`، وكود التسجيل (`MediaRecorder`) وكل مراجع الويب. **طبقة التخزين (Entity/DAO/@Database) تُركت كما هي عمدًا** فلا ترقية مخطط ولا هجرة مدمّرة تُتلف الإشارات والمواقع. والدرج كان يفتح بأي 25px صعودًا في أي مكان (تمرير قائمة يفتحه) — صار يتطلب سحبة تبدأ من الخُمس السفلي (`DRAWER_EDGE_FRACTION`) وتقطع `DRAWER_PULL_PX`، على الطبقتين. اختبار عقد يمنع رجوع `voice_notes`. 47/47 + 10/10.
- [2026-09-02] **أُلغي بند التسجيلات الصوتية في الويب** بطلب المستخدم قبل كتابة أي كود. الفحص كان قد أظهر أن `LocalSyncServer` لا يتعامل مع التسجيلات إطلاقًا، وأن الصوت (`filePath`) محلي على الساعة فلا يعبر مُرحّل JSON — فأقصى ما كان ممكنًا مزامنة بيانات وصفية فقط. لم تتغير أي ميزة على الساعة.
- [2026-09-02] **أ3 معاينات الواجهات التسع بقيم حيّة + ب1 المواقع والإشارات تُزامَن**: معاينات الويب للتسع صارت تقرأ من `webFaceData()` (وقت/يوم/هجري/عدّاد صلاة حقيقي بدل 12:45 المجمّد)، وواجهة `QURANIC_AMBIENT_ORBIT` استعادت سطر الآية. مدير مواقع محفوظة حقيقي في تبويب المواقع (قائمة المستخدم + تفعيل/حذف، والمدن الجاهزة صارت أزرار إضافة، وزر ＋ لموقع مخصّص)، و`syncAll` صار يرسل `locations` و`bookmarks` بالشكل الذي يوفّق عليه `importDataJson`، و`pullFromCloud` يستعيدهما. الاختبارات 47/47.
- [2026-09-02] **أ2 لا يحتاج شيئًا + أ5 «فسيفساء المؤمن» بسّطت**: مسحُ الواجهات التسع أكّد أنها داكنة أصلًا وتشارك chrome التعقيدات الهادئ، والتدرّج البرتقالي‑الأحمر الوحيد مقيّد بـ`isAlertActive` (تنبيه وظيفي). «فسيفساء المؤمن» صارت 3 أشرطة (رأس طقس · قلب 3 بطاقات · تذييل قراءة+تسبيح) بدل 4، بنفس الخانات الأربع. الاختبارات 47/47.
- [2026-09-03] **«نظرة عامة» صادقة + مؤشر تنقّل**: بطاقات النظرة العامة كانت تطبع أرقامًا لم تُحسب قط (78٪ بطارية، «12 آية»، عدّاد صلاة مجمّد) — استُبدلت بما يعرفه المتصفح فعلًا (عدد البلاطات وشكلها، الموقع وطريقة الحساب، عدد الإشارات، وقت آخر مزامنة وحالة تخزينها)، والمجهول يقول «—». وأُضيفت نقطتا صفحة أسفل الواجهات الـ15 تقولان إن هناك شاشة ثانية، ونقرهما يفتح البلاطات (الاختبارات 47/47).
- [2026-09-03] **توحيد اللغة البصرية — الخطوة 3 (معاينات الويب)** (ملتزم `d8956db`): `.wf-comp-badge` (بادج المُركَّبات في معاينة الواجهات التسع الأصلية بالويب) خلفية `#111827` + حدّ `#1f2937` المشبعة → `#0C1319` (TilePanel) + `rgba(255,255,255,.12)` (مرآة `ComplicationHairline`). `.wf-emblem-badge` خلفية `#1e293b` → `#0C1319`. ضبط دقيق للواجهات الست: قوس المحراب `.45→.55`، حلقة القبلة `rgba(53,217,255,.5)→rgba(52,217,255,.7)`، دائرة عدّاد التسبيح `#f3c969→rgba(243,201,105,.7)` — كلها مرآة قيم `NewWatchFaces.kt` (Aqua/Cyan/Gold عند 0.55f/0.7f/0.7f). اختبار عقد جديد في `watchfaces-v2-contract.test.mjs` يثبّت المرآة ويمنع رجوع `#111827`. **47/47**. تحقُّق بصري في المتصفح على `CHRONO_HERITAGE`: 4 بادجات `rgb(12,19,25)` + حدّ `rgba(255,255,255,0.12)`. لم يُمَس أي كود Kotlin.
- [2026-09-03] **توحيد اللغة البصرية — الخطوة 2 (الواجهات التسع الأصلية)**: `RenderComplicationContent` المشترك + بادج تنبيه `UltraDigitalFaceView` — خلفية قاتمة ثابتة (`0xFF0F172A`/`0xFF1E293B`) → `TilePanel` + حدّ شعري جديد `ComplicationHairline`. حلقة `SolarHorizonFullFaceView` الزخرفية الدائمة (سماوي/ذهبي حتى بلا تنبيه) → خط `AccentGold` هادئ 35٪؛ تدرّج التنبيه الكهرماني/الأحمر أُبقي كإشارة وظيفية. `compileDebugKotlin` نظيف، `testDebugUnitTest` 10/10، `assembleDebug` نجح — **لم يُتحقَّق على الجهاز بعد** (الساعة غير متصلة حاليًا). ملتزم `94b867d`.
- [2026-09-03] **البلاطات: ملتحمة + تعرض معلوماتها**: تغيير تصميم بحت — كل بلاطة تحتفظ بوظيفتها ونقرها وضغطها المطوّل. `HomeScreen` يجمع الموقع/الإحداثيات/الإشارات ويشتقّ اتجاه القبلة عبر `qiblaBearing` المشترك؛ الحدّ ينخفض 55٪ للشكل المتصل؛ `getPreviewLabel` يعكس القيم الجديدة؛ اختبار عقد يثبّتها (46/46).
- [2026-09-03] **الخطوة 1 مُعايَنة على الساعة**: ثُبّت البناء الصحيح (كان أول تثبيت بـAPK قديم فظهرت البلاطات مشبعة كما هي) والتُقطت لقطة قبل/بعد — النظام الهادئ يعمل على `SM_L500`.
- [2026-09-03] **توحيد اللغة البصرية — الخطوة 1 (البلاطات)**: `TilePanel #0C1319` + `TILE_TINT_ALPHA 0.14` + `TILE_BORDER_ALPHA 0.38` مشتركة بين `HomeScreen.kt` و`tileSurface()` في `app.js`؛ أرضيات الأنماط هبطت لشبه الأسود على الطرفين؛ تجاوزات `icon-palette-*` تتبع نفس القاعدة؛ اختبار عقد جديد يثبّت الأرقام (46 اختبارًا). سبقه: صقل الواجهات الست (ملء الشاشة، تواريخ حقيقية في التعقيدات، إصلاح قصّ تسميات «مدارات اليوم»، اتجاه عدّاد السبحة)، ودرج التطبيقات (صفّ الأخيرة + زر ⌂). ونُشر مستند قرار التصميم.
- [2026-09-03] **مراجعة عمل وكيل الواجهات الست + معاينات ويب حيّة** (مدفوع `5823cae`, `78695b4`): راجعت تغييرات وكيل آخر أضاف ٦ واجهات عربية (`FAJR_MIHRAB`…`BELIEVER_MOSAIC`، ١٥ إجمالًا) — عمل منضبط: منطق نقي مفصول في `watchfaces/WatchFaceCalculations.kt`، أول اختبارات JVM (`app/src/test/`، ١٠/١٠)، اختبار عقد ويب↔ساعة (`watchfaces-v2-contract.test.mjs`)، بيانات حيّة تحل محل القيم المزيّفة، `slotProfiles` (ذاكرة تعقيدات لكل نموذج) على الطرفين، `WeatherSnapshot` مبنيّة. commit كما هو. ثم: `renderNewWatchFacePreview` صار يقرأ من `webFaceData()` واحد — الوقت/اليوم/الهجري حيّة، وبقية القيم التمثيلية معرّفة مرة وتُعاد عبر الست، ومتّسقة مع `getComplicationPresentation`. الاختبارات ٤٥/٤٥. **ثُبِّت على الساعة SM_L500 (2026-09-02 12:56) وتُحقِّق بصريًا**: FAJR_MIHRAB (قوس محراب Canvas + جدول مواقيت حقيقي + هجري حيّ)، DAILY_ORBITS (أقواس + بطارية/صلاة حيّة)، QIBLA_SERENITY — كلها تعرض بيانات حيّة بلا أعطال، والمزامنة تعمل. المتبقّي: مطابقة معاينة الواجهات التسع الأصلية في الويب.
- [2026-09-03] **تطابق البلاطات ويب↔ساعة + درج فوري + ⚠️ تحدّي Vercel** (مدفوع `3614951`): عامل `renderScale = العرض/438` مشترك حرفيًا بين `renderCanvas` و`SmartWatchFaceTile`؛ نصوص العناوين موحّدة (`getPreviewLabel` = `displayTitle`)؛ نموذج تموضع `translate(-x%,-y%)` = `BiasAlignment`؛ الساعة تحترم `textAlign` الداخلي بدل توسيط ثابت؛ `clock_big` يُظهر AM/PM في المحاكي؛ `square-gapped` 10dp؛ `date_big` تاريخ حيّ. درج التطبيقات: كاش `InstalledAppsCache` + تحميل على مرحلتين. تحقّق بصري A/B على `SM_L500`: الألوان والأشكال والعناوين والمواضع متطابقة. **⚠️ تنبيه**: كثرة نداءات الكيرل على `/api/sync` هذه الجلسة فعّلت «Attack Challenge Mode» على مشروع Vercel — الموقع كله يرد `403 Security Checkpoint` للعملاء غير المتصفّح (بما فيهم `HttpURLConnection` للساعة)، ما يعطّل مزامنة الساعة حتى يزول. يزول تلقائيًا خلال ~ساعة، أو يُطفأ يدويًا من Vercel → Firewall.
- [2026-09-03] **رأس صف واحد + تحريك النص + تكبير سلس + تحقّق المزامنة** (غير ملتزم بعد): (١) رأس صفحة البلاطات انطوى في صف واحد (`#tab-tiles .section-heading`: h2 17px، الفقرة مخفية) لأن الشريط العلوي العام يحمل العنوان والوصف وزر «تطبيق». (٢) **تحريك النص داخل البلاطة صار يعمل**: كان `dragType='text'/'icon'` كودًا ميتًا لأن `handleStart` لا يقرأ `data-role`؛ أُضيف فرع يبدأ سحب النص/الأيقونة عند الضغط على عنصر البلاطة المحدّدة نفسها. (٣) **تكبير سلس**: منزلق `#tileFontSizeRange` (8–48) مع `applyFontSize()` — سحب حيّ يعيد رسم الساعة فقط (`renderCanvas(true)` يتخطى قائمة الطبقات والمعاينة المصغّرة) ويثبّت التاريخ/المزامنة عند الإفلات. (٤) **المزامنة**: تأكيد بالكيرل أن `quran-watch8-hub.vercel.app/api/sync` يعمل تمامًا (POST→`vercel-kv`، GET يرجع `tilesConfig.tiles`) — **الفجوة في الساعة، لا السحابة**. تقوية `LocalSyncServer.importDataJson`: لا يستبدل تخطيط الساعة إلا إذا حمل payload مصفوفة `tiles` غير فارغة (كان يمسح التصميم بالافتراضي الستّة عند فشل التحليل). أُعيد بناء APK (`app-debug.apk`، 07:14). الاختبارات 38/38.
- [2026-09-03] **المصمم: طبقات أسفل الساعة + لوحة خصائص بديلة + ألوان منسدلة** (غير ملتزم بعد): قائمة الطبقات نُقلت من قائمة منسدلة تُقصّ خارج الشاشة إلى لوحة ثابتة `#designerLayersPanel` أسفل الساعة، وحُذف زر `btnOpenLayersPanel` و`designerLayersMenu`. قائمتا الإضافة/المزيد تفتحان للأسفل الآن. لوحة الخصائص: كل الأقسام موسّعة افتراضيًا + زر «طيّ/توسيع الكل» + مربّعات إظهار/إخفاء لكل قسم، محفوظة في `localStorage`. الألوان الثلاثة صارت `<select>` من `DESIGNER_PALETTE` مع `normalizeHex()`. حُذفت ~110 أسطر CSS ميتة (سُلّم المصمم القديم). الملفات: `pwa-web/{index.html,app.js,style.css,tests/ui-contract.test.mjs}`. تحقّق: `node -c app.js` سليم، `node --test` = **38/38**، وتحقّق بصري في المتصفح (الطي/التوسيع، إخفاء قسم، تغيير لون، فتح القوائم بلا قصّ).
- [2026-09-03] **تصحيح المصمم: الخصائص في موضع الطبقات — منشور**: استجابة لملاحظة المستخدم، أزيلت نافذة الخصائص العائمة وطبقة التعتيم من HTML وJavaScript وCSS. `designerInspectorPanel` ثابتة في موضع شريط الطبقات القديم، فيما `designerLayersMenu` قائمة منسدلة في شريط الأوامر فوق المعاينة. اختيار طبقة من القائمة يغلقها فقط ويحدّث الخصائص؛ اختيار عنصر من الساعة لا يفتح أي غطاء، فتظل المقابض والسحب مرئية. على الشاشات الضيقة تظهر الخصائص أسفل المعاينة بدلاً من غطائها. الملفات: `pwa-web/index.html` و`app.js` و`style.css` و`tests/ui-contract.test.mjs`. التحقق المحلي: `node --check pwa-web/app.js` و`node --test pwa-web/tests/*.test.mjs` = **37/37**، وفحص المعرّفات = **142 فريدًا**. رُفع التنفيذ إلى `main` في `0f230ed`، واكتمل نشر Vercel بنجاح على `https://quran-watch8-hub.vercel.app/`. فحص الإنتاج أكد وجود `designerInspectorPanel` و`designerLayersMenu`، واستجابة المزامنة بقيت `storage:"vercel-kv"`.
- [2026-09-03] **المصمم: الساعة أولًا والخصائص في درج — استُبدل**: كان هذا تنفيذًا أوليًا للدرج الحواري؛ استُبدل مباشرةً بالسجل الذي يسبقه لأن المستخدم طلب ألا يغطي أي جزء من المعاينة. بقيت أقسام الخصائص نفسها لكن في اللوحة الثابتة.
- [2026-09-02] **إعادة هيكلة الإيماءات + تصفية بنود المراجعة** (`a8bf069`): الطبقة الأولى — سحب لليسار = تقدّم، سحب للأعلى = الدرج، وسحب لليمين/لأسفل للنظام (كان السحب يمينًا يخرج من التطبيق). مزامنة حذف الإشارات/المواقع في `importDataJson` مع حارس `timestamp`. تسجيل بدل `catch` الفارغة. حذف `getWatchIpAddress`/`rebalanceRowHeights`. استعادة `appearance.tileShape` في `SmartWatchFaceTile`. **الاختبارات 36/36**.
- [2026-09-02] **إصلاح الأعطال الثلاثة** (`d4b4be3`): `util/HijriDate.kt` (تاريخ هجري حيّ بديلًا عن نص ثابت في 5 مواضع)، الراندرر يتابع الموقع المحفوظ بدل `-34.5721,-58.4239`، ومعاينة الويب بأرقام لاتينية (`ar-EG-u-nu-latn`). زيادة: `@Volatile` و`onDestroy` في الراندرر.
- [2026-09-02] **إعادة ترتيب الطبقتين (خطة الطبقة الأولى)**: قفل 3 خانات تعقيدات وإبقاء السفلى قابلة للتبديل باللمس (`FLEX_SLOT` + خط كهرماني)، تقليم محرر البلاطات على الساعة إلى `assignableTiles` (23 بدل 50)، تبسيط بلاطة المجلد إلى أيقونة+عدد في الويب، استبدال ساعة البلاطات بالعدّ التنازلي العريض، وتوحيد افتراضيات `watchFaceConfig`. `248528a` وما قبله.
- [2026-09-02] **إغلاق ملف المزامنة السحابية**: ربط `quran-watch8-hub` بمستودع GitHub، تفعيل KV، إصلاح 404 على `/api/*` بجسر `api/sync.js` جذري، وإصلاح `vercel.json` بعد أن حوّله وكيل Vercel إلى `routes` (عطّل `headers`/`cleanUrls` و404 على `/`). الإنتاج أخضر: `/` و`/api/sync` و`storage:"vercel-kv"`.
- [2026-09-01] **ربط KV والتحقق منه في الإنتاج، وإصلاح استنزاف البطارية**: `storage:"vercel-kv"` يعمل في الإنتاج (جسر `api/sync.js` جذري + `.vercelignore`)؛ `QuranWatchFaceRenderer` من 16ms إلى 1000ms مع نبضة تكيّفية، وإزالة `FLAG_KEEP_SCREEN_ON`. مُتحقَّق على `SM_L500`.
- [2026-09-01] **إطلاق استوديو الطبقة الأولى + درج التطبيقات + إدارة الإشارات**: أول نسخة من محرر الويب (9 نماذج + محاكي حي)، درج التطبيقات، والإشارات المرجعية والقوالب — أساس كل ما بُني عليه لاحقًا.
