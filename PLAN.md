# خطة العمل — قائمة حيّة

تُحدَّث بعد كل بند يُنجَز. `[x]` = تمّ ومُلتزم، `[~]` = قيد العمل، `[ ]` = لم يبدأ.
آخر تحديث: 2026-09-03 (أُنجزت أ/ب/ج/ز/ح — الخطوة 3 من توحيد اللغة البصرية: معاينات الويب).

---

## أ. خطة الطبقات (ترتيب الميزات)

- [x] تقليم محرر البلاطات على الساعة (50 → 23 وجهة قابلة للإسناد) — `98bf1bd`
- [x] توحيد افتراضيات `watchFaceConfig` بين Kotlin والويب — `14085a4`
- [x] بلاطة المجلد: أيقونة + عدد بدل 4 أيقونات مبتورة — `98bf1bd`
- [x] بلاطات الطبقة الثانية تفتح بالعدّ التنازلي بدل ساعة مكرّرة — `248528a`
- [x] الخانة المرنة: قفل 3 خانات، السفلى تدور باللمس + خط كهرماني — `14085a4` (مُتحقَّق على الساعة)
- [x] **إعادة هيكلة الإيماءات** — الطبقة الأولى: سحب لليسار = تقدّم للبلاطات، سحب للأعلى = الدرج، وسحب لليمين/لأسفل تُركا للنظام (رجوع، إعدادات سريعة). جدول المواقيت انتقل لضغطة مطوّلة على المصحف أو تعقيدة الصلاة. الطبقة الثانية: سحب لليمين = رجوع للواجهة.

## ب. أعطال مراجعة التصميم (الثلاثة العاجلة)

- [x] **إحداثيات المواقيت الثابتة** — الراندرر يتابع `selectedLat/Lng/method` من DataStore ويُعيد الحساب عند التغيير وكل 15د.
- [x] **التاريخ الهجري الثابت** — `util/HijriDate.kt` جديد (Umm al-Qura)، استُبدلت النصوص اليدوية في الراندرر (×2) و`WatchFaceHomeScreen` (×3).
- [x] **أرقام هندية في معاينة الويب** — `getPreviewLabel` يستعمل `ar-EG-u-nu-latn`.

## ج. بنود تقنية أصغر

- [x] `@Volatile` على `currentConfig`/`lastPrayerResult` في الراندرر — `d4b4be3`
- [x] `onDestroy { scope.cancel() }` في الراندرر — `d4b4be3`
- [x] مزامنة حذف الإشارات/المواقع — `importDataJson` يوفّق: يحذف ما اختفى من الحمولة، ويستثني ما أُضيف على الساعة بعد بناء الحمولة (`timestamp < payloadVersion`).
- [x] `catch` فارغة في `LocalSyncServer` — أُضيف `Log.w/e` بوسم `LocalSyncServer`.
- [x] كود ميت: `getWatchIpAddress()` — حُذف. (`rebalanceRowHeights` حُذف؛ `rebalanceRowWidths` أُبقِي لأنه مستعمل في أزرار colSpan.)
- [x] الاختبارات الثلاثة: `maxLines` 2→3 لبلاطة القراءة، استعادة قراءة `appearance.tileShape` في `SmartWatchFaceTile` (سدّ فجوة ويب↔ساعة)، وإصلاح خطأ في اختبار الشريط (`.watch-stage` مزدوج). **36/36 يمرّ.**

## ز. توحيد اللغة البصرية — الخطوة 2 (الواجهات التسع الأصلية)

- [x] بادج المُركَّبات المشتركة (`RenderComplicationContent`، تستعملها كل الواجهات التسع): خلفية `Color(0xFF0F172A)` ثابتة → `TilePanel` + حدّ شعري `ComplicationHairline` — نفس نظام البلاطات.
- [x] بادج التنبيه في `UltraDigitalFaceView`: خلفية `Color(0xFF1E293B)` ثابتة → `TilePanel` + حدّ شعري (النص الكهرماني بقي كإشارة الاستعجال).
- [x] حلقة `SolarHorizonFullFaceView` الزخرفية الدائمة (تدرّج سماوي/ذهبي حتى في الحالة الهادئة) → خط واحد هادئ `AccentGold` بشفافية 35٪ عند الهدوء؛ تدرّج التنبيه الكهرماني/الأحمر أُبقي كما هو لأنه إشارة وظيفية (اقتراب الصلاة)، وليس زخرفة.
- [ ] لم تُمس: أيدي الساعة التناظرية، أرقام الميناء (`#FDE68A`)، شعار المصحف — هذه طباعة/رسم وظيفي لا خلفيات/حدود، خارج نطاق "التوحيد البصري".
- **التحقق**: `compileDebugKotlin` نظيف (تحذيرات قديمة فقط)، `testDebugUnitTest` = 10/10، `assembleDebug` نجح. **عوينت على `SM_L500` وتمّت**: ثُبِّت الـAPK وشُغِّل، وتحقَّقت بصريًا من 3 واجهات (الرقمي اللاتيني، الكرونوغراف التراثي، الأفق الشمسي) — كل بادجات المُركَّبات لوحة داكنة + حدّ شعري بدل الخلفية القاتمة السابقة، وحلقة «الأفق الشمسي» صارت خطًا ذهبيًا هادئًا واحدًا بدل التدرّج السماوي/الذهبي الدائم السابق.

## ح. توحيد اللغة البصرية — الخطوة 3 (معاينات الويب للـ15 واجهة)

- [x] معاينة الواجهات التسع الأصلية في الويب (`.wf-comp-badge` في `renderLiveWatchFacePreview`): خلفية `#111827` + حدّ `#1f2937` المشبعة → `#0C1319` (TilePanel) + حدّ `rgba(255,255,255,.12)` (مرآة `ComplicationHairline` = `0x1FFFFFFF`). بادج الشعار المركزي `.wf-emblem-badge`: خلفية `#1e293b` → `#0C1319` (الحدّ الذهبي بقي كإشارة «البلاطات المتصلة»).
- [x] ضبط دقيق لمعاينات الواجهات الست الجديدة على قيم الساعة: قوس المحراب `.45→.55` (= `Aqua 0.55f`)، حلقة القبلة `rgba(53,217,255,.5)→rgba(52,217,255,.7)` (= `Cyan 0.7f`)، دائرة عدّاد التسبيح `#f3c969→rgba(243,201,105,.7)` (= `Gold 0.7f`).
- [x] اختبار عقد جديد في `watchfaces-v2-contract.test.mjs` يثبّت مرآة `.wf-comp-badge` ↔ `TilePanel`+`ComplicationHairline` ويمنع رجوع `#111827`. **الاختبارات 47/47.**
- **التحقق**: `node --test pwa-web/tests/*.test.mjs` = 47/47؛ فحص بصري في المتصفح على `CHRONO_HERITAGE` — البادجات الأربعة `rgb(12,19,25)` + حدّ `rgba(255,255,255,0.12)` مطابقة للحوسبة على الساعة.

## د. مؤجَّل بقرار صريح

- CSS: دمج كتل `@media` — أُثبت أنه محفوف بالمخاطر وتُرك كما هو
- ربط استوديو لوحة التطبيق بوجه ساعة النظام — القرار: "توضيح فقط" (أُعيدت التسمية)

## هـ. التحقق على الساعة (`SM_L500`)

- [x] APK يثبّت ويقلع بلا أعطال
- [x] **التاريخ الهجري حيّ**: `19 Rabi I 1448` (كان "Safar" ثابتًا) — مع إصلاح LRM لترتيب RTL
- [x] **عدّاد المواقيت مُصلَح**: `409m` لفجر الغد (كان `0m`)، وقوس التنبيه اختفى — اكتُشف عطل رابع (دوران اليوم بعد العشاء) وأُصلح في `PrayerTimesHelper` — `03aece8`
- [x] الخانة المرنة (خط كهرماني + تدوير باللمس) ولمس الخانات المقفلة (نافذة تفاصيل) — مُتحقَّق سابقًا
- [ ] الإيماءات الجديدة (سحب لليسار → بلاطات) — لم تُتحقَّق: `adb input swipe` لا يُشغّل `detectDragGestures` بموثوقية؛ يحتاج إصبعًا حقيقيًا
- [x] أشكال البلاطات — الآن معروضة في canvas الويب أيضًا (كانت تُفرَض مربعة دائمًا). مُتحقَّق بصريًا: `mixed` يبدّل square/oval بالتناوب.

## و. مطابقة الويب لتعديلات الساعة (`13e2e63`)

- [x] canvas الويب يعرض شكل البلاطة المختار (oval/mixed/circle) بدل مربع دائمًا — يطابق `SmartWatchFaceTile`
- [x] معاينة الطبقة الأولى + نص عيّنة `HIJRI_DATE` تحسب تاريخ اليوم واليوم الأسبوعي (كانت ثابتة `الجمعة · 18 ربيع الأول`؛ الآن `الثلاثاء · 19 ربيع الأول`)

---

## المنجَز سابقًا في الجلسة (مرجع)

- البطارية: راندرر 16ms→1000ms، حذف `KEEP_SCREEN_ON`، نبضة تكيّفية، إزالة أنيميشن 250/ث — `eee86f3` (مُتحقَّق على الساعة)
- تحجيم البلاطات: 4 عيوب + `resize-grid.test.mjs` — `983bbfb`
- المزامنة: نقل إلى Vercel KV، حذف قناة LAN والمسار الميت، جسر `api/sync.js` جذري، إصلاح `vercel.json` — `f096e7e`→`bb87b3e` (حيّ: `storage:"vercel-kv"`)
