# خطة العمل — قائمة حيّة

تُحدَّث بعد كل بند يُنجَز. `[x]` = تمّ ومُلتزم، `[~]` = قيد العمل، `[ ]` = لم يبدأ.
آخر تحديث: 2026-09-02 (بعد إصلاح الأعطال الثلاثة).

---

## أ. خطة الطبقات (ترتيب الميزات)

- [x] تقليم محرر البلاطات على الساعة (50 → 23 وجهة قابلة للإسناد) — `98bf1bd`
- [x] توحيد افتراضيات `watchFaceConfig` بين Kotlin والويب — `14085a4`
- [x] بلاطة المجلد: أيقونة + عدد بدل 4 أيقونات مبتورة — `98bf1bd`
- [x] بلاطات الطبقة الثانية تفتح بالعدّ التنازلي بدل ساعة مكرّرة — `248528a`
- [x] الخانة المرنة: قفل 3 خانات، السفلى تدور باللمس + خط كهرماني — `14085a4` (مُتحقَّق على الساعة)
- [ ] **إعادة هيكلة الإيماءات** — محور واحد للتقدّم، تحرير إيماءة الرجوع والإعدادات السريعة للنظام، ونقل جدول المواقيت إلى ضغطة مطوّلة. (مؤكَّد معطوب حيًّا: السحب يمينًا يخرج من التطبيق.)

## ب. أعطال مراجعة التصميم (الثلاثة العاجلة)

- [x] **إحداثيات المواقيت الثابتة** — الراندرر يتابع الآن `selectedLat/Lng/method` من DataStore ويُعيد الحساب عند التغيير وكل 15د لدوران اليوم. `-34.5721,-58.4239` أُزيلت.
- [x] **التاريخ الهجري الثابت** — `util/HijriDate.kt` جديد (Umm al-Qura عبر `java.time.chrono.HijrahDate`)، واستُبدلت النصوص المكتوبة يدويًا في الراندرر (×2) و`WatchFaceHomeScreen` (×3؛ نص "الأيام البيض" تُرك لأنه محتوى مرجعي ثابت لا تاريخ اليوم).
- [x] **أرقام هندية في معاينة الويب** — `getPreviewLabel` يستعمل `ar-EG-u-nu-latn` فيبقي أسماء الشهور عربية ويحوّل الأرقام لاتينية (`٠٦:٥٢` → `18:52`).

## ج. بنود تقنية أصغر (من المراجعة الشاملة، مفتوحة)

- [x] `@Volatile` على `currentConfig`/`lastPrayerResult` في الراندرر — أُضيف مع إصلاح الإحداثيات
- [x] نطاق كوروتين الراندرر لا يُلغى — أُضيف `override fun onDestroy() { scope.cancel() }`
- [ ] لا مزامنة حذف للإشارات/المواقع (حذفها على الويب لا يحذفها على الساعة)
- [ ] `catch` فارغة في مسار المزامنة في Kotlin — إخفاء أخطاء
- [ ] كود ميت: `LocalSyncServer.getWatchIpAddress()` (بلا مستدعٍ بعد حذف LAN)
- [ ] كود ميت: `rebalanceRowHeights()` في `app.js` (بلا مستدعٍ؛ باقٍ فقط لاختبار نصّي)
- [ ] قرار الاختبارات الثلاثة الفاشلة: تحديثها أم إعادة الميزات (`maxLines=3`, `tileShape=="mixed"`, موضع زر الترتيب التلقائي)

## د. مؤجَّل بقرار صريح

- CSS: دمج كتل `@media` — أُثبت أنه محفوف بالمخاطر وتُرك كما هو
- ربط استوديو لوحة التطبيق بوجه ساعة النظام — القرار: "توضيح فقط" (أُعيدت التسمية)

---

## المنجَز سابقًا في الجلسة (مرجع)

- البطارية: راندرر 16ms→1000ms، حذف `KEEP_SCREEN_ON`، نبضة تكيّفية، إزالة أنيميشن 250/ث — `eee86f3` (مُتحقَّق على الساعة)
- تحجيم البلاطات: 4 عيوب + `resize-grid.test.mjs` — `983bbfb`
- المزامنة: نقل إلى Vercel KV، حذف قناة LAN والمسار الميت، جسر `api/sync.js` جذري، إصلاح `vercel.json` — `f096e7e`→`bb87b3e` (حيّ في الإنتاج: `storage:"vercel-kv"`)
